If there is something that says authentication failed

then you need a Personal Access token, i recommend researching this on your own, i am tired.

if `src refspec main does not match any`
use `git branch`

`fatal:remote origin already exists`
use 
```
git remote -v
or
got remote remove origin
```
then add again

if it says `nothing to commit`
run `git status`
it will tell you if there are changes

Remember
commit = saved locally
push = upload

